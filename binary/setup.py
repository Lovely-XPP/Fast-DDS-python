import setuptools
setuptools.setup(
    name='fastdds',
    version='2.1.0',
    description='Python bindings for Fast DDS.',
    author='',
    author_email='',
    packages=setuptools.find_packages(),
   	include_package_data=True
)
